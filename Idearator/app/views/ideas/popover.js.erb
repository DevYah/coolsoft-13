$('a[data-popover-idea-id]').popover({
  animation: true,
  html: true,
  content:'<%= escape_javascript(render(:partial => "popover")) %>',
  container: 'body'
});

$('a[data-popover-idea-id]').popover('show')