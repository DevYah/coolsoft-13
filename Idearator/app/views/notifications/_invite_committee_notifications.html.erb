<div class = 'well notification invite-committee <%= notification.read_by?(current_user) ? 'read' : 'unread' %>' data-notification = <%= notification.id %> data-time=<%= notification.created_at.to_i %>>
  <span><%= notification.text %></span>
  <div class = 'link'><%= link_to 'choose area of expertise', {controller: 'users', action: 'send_expertise'}, :remote => true %></div>
  <span class = 'date'><%= notification.created_at.to_s(:long)%></span>
</div>