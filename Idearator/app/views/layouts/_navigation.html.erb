<%= link_to "Home", root_path, :class => 'brand' %>
<ul class="nav">
  <% if user_signed_in? %>
    <li>
    <%= link_to('Sign out', destroy_user_session_path, :method=>:delete) %>
    </li>
  <% end %>
  <% if user_signed_in? %>
    <li>
    <%= link_to('Deactivate', controller: 'users' , action: 'confirm_deactivate') %>
    </li>
  <% end %>
</ul>
<ul class="nav pull-right">
  <% if user_signed_in? %>
  <li>
  <%= link_to('Login via Twitter', user_omniauth_authorize_path(:twitter)  %>
  </li>
  <li>
  <%= link_to('Register', new_user_session_path)  %>
  </li>
  <% end %>
</ul>
