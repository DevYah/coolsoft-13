<div id="expertise" class="modal hide fade in" style="display: none; ">  
<div class="modal-header">  
<a class="close" data-dismiss="modal">×</a>
<h3>Please choose your area of expertise :</h3>
</div>  
<div class="modal-body">
	<%= form_for(@user, url: url_for(controller: 'users', action: 'new_committee_tag')) do |form| %>
		<% @tags.each do |tag|%>
	    	<div><%= check_box_tag 'user[tags][]', tag.id, false %><%= tag.name %> </div>
		<% end %>	
</div>
<div class="modal-footer">  
	<%= form.submit  value= 'Submit' %>
	<% end %>  
<%= link_to "Reject", :controller => "users", :action => "reject_invitation", :id => @user, :class => "btn" %>
</div>  
</div>